(()=>{const b=window.JURISITU_BASE;if(!b)return;const painel=document.getElementById('painel');if(painel){const box=document.createElement('div');box.className='card';box.style.marginTop='13px';box.innerHTML=`<h2>Base processual importada</h2><div class="grid"><div class="kpi"><span class="muted">Registros da planilha</span><b>${b.registrosPlanilha.toLocaleString('pt-BR')}</b></div><div class="kpi"><span class="muted">Processos indexados</span><b>${b.processosIndexados.toLocaleString('pt-BR')}</b></div><div class="kpi"><span class="muted">Processos no boletim teste</span><b>${b.processosUnicosBoletim}</b></div><div class="kpi"><span class="muted">Já localizados na base</span><b>${b.localizadosNaBase}</b></div></div><p class="muted">Primeiro cruzamento realizado em ${b.atualizadoEm}. Os números ainda serão refinados pelos filtros de relevância da triagem.</p>`;painel.appendChild(box)}const proc=document.getElementById('processos');if(proc){const box=document.createElement('div');box.className='card';box.style.marginTop='12px';box.innerHTML='<h3>Exemplos já reconhecidos no cruzamento Grifon × planilha</h3><table><thead><tr><th>Processo</th><th>Parte</th><th>Responsável atual</th><th>Assunto</th></tr></thead><tbody>'+b.amostra.map(r=>`<tr><td>${r[0]}</td><td>${r[1]}</td><td><span class="badge">${r[2]}</span></td><td>${r[3]}</td></tr>`).join('')+'</tbody></table>';proc.appendChild(box)}})();